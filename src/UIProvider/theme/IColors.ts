export type Theme = "light" | "dark";

export type IColors = {
    text_inverse: string,
    text: string;
    background: string;
    card: string;
    notification: string;
    border: string;
    primary: string;
    button: string;
};